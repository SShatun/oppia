{
  "CodeRepl": {
    "CodeEquals": {
      "description": "код, равный {{x|CodeString}}"
    },
    "CodeContains": {
      "description": "код, содержащий {{x|CodeString}}"
    },
    "CodeDoesNotContain": {
      "description": "код, не содержащий{{x|CodeString}}"
    },
    "OutputEquals": {
      "description": "вывод равен {{x|CodeString}}"
    },
    "OutputContains": {
      "description": "вывод содержит {{x|CodeString}}"
    },
    "ResultsInError": {
      "description": "приводит к ошибке при запуске"
    },
    "ErrorContains": {
      "description": "сообщение об ошибке, содержащее {{x|UnicodeString}}"
    }
  },
  "Continue": {},
  "EndExploration": {},
  "GraphInput": {
    "IsIsomorphicTo": {
      "description": "изоморфен {{g|Graph}}, включая соответствующие метки"
    }
  },
  "ImageClickInput": {
    "IsInRegion": {
      "description": "находится в области {{x|UnicodeString}}"
    }
  },
  "InteractiveMap": {
    "Within": {
      "description": "находится в {{d|Real}} км от {{p|CoordTwoDim}}"
    },
    "NotWithin": {
      "description": " не находится в {{d|Real}} км от {{p|CoordTwoDim}}"
    }
  },
  "ItemSelectionInput": {
    "Equals": {
      "description": "равно {{x|SetOfHtmlString}}"
    },
    "ContainsAtLeastOneOf": {
      "description": "содержит хотя бы один из {{x|SetOfHtmlString}}"
    },
    "DoesNotContainAtLeastOneOf": {
      "description": "опускает хотя бы один из {{x|SetOfHtmlString}}"
    }
  },
  "LogicProof": {
    "Correct": {
      "description": "верно"
    },
    "NotCorrect": {
      "description": "не верно"
    },
    "NotCorrectByCategory": {
      "description": "неверно из-за {{c|LogicErrorCategory}}"
    }
  },
  "MathExpressionInput": {
    "IsMathematicallyEquivalentTo": {
      "description": "математически эквивалентно (LaTeX) {{x|UnicodeString}}"
    }
  },
  "MultipleChoiceInput": {
    "Equals": {
      "description": "равно {{x|NonnegativeInt}}"
    }
  },
  "MusicNotesInput": {
    "Equals": {
      "description": "равно {{x|MusicPhrase}}"
    },
    "IsLongerThan": {
      "description": "более чем {{k|NonnegativeInt}} нот"
    },
    "HasLengthInclusivelyBetween": {
      "description": "между {{a|NonnegativeInt}} и {{b|NonnegativeInt}} нот, включительно"
    },
    "IsEqualToExceptFor": {
      "description": "равно {{x|MusicPhrase}} кроме {{k|NonnegativeInt}} нот"
    },
    "IsTranspositionOf": {
      "description": "является транспозицией {{x|MusicPhrase}} по {{y|Int}} полутонов"
    },
    "IsTranspositionOfExceptFor": {
      "description": "является транспозицией {{x|MusicPhrase}} по {{y|Int}} полутонов кроме {{k|NonnegativeInt}} нот"
    }
  },
  "NumericInput": {
    "Equals": {
      "description": "равно {{x|Real}}"
    },
    "IsLessThan": {
      "description": "меньше чем {{x|Real}}"
    },
    "IsGreaterThan": {
      "description": "больше чем{{x|Real}}"
    },
    "IsLessThanOrEqualTo": {
      "description": "меньше или равно {{x|Real}}"
    },
    "IsGreaterThanOrEqualTo": {
      "description": "больше или равно {{x|Real}}"
    },
    "IsInclusivelyBetween": {
      "description": "между {{a|Real}} и {{b|Real}}, включительно"
    },
    "IsWithinTolerance": {
      "description": "внутри {{tol|Real}} {{x|Real}}"
    }
  },
  "PencilCodeEditor": {
    "CodeEquals": {
      "description": "код, равный {{x|CodeString}}"
    },
    "CodeContains": {
      "description": "код, содержащий {{x|CodeString}}"
    },
    "CodeDoesNotContain": {
      "description": "код, не содержащий{{x|CodeString}}"
    },
    "OutputEquals": {
      "description": "вывод равен {{x|CodeString}}"
    },
    "OutputContains": {
      "description": "вывод содержит {{x|CodeString}}"
    },
    "ResultsInError": {
      "description": "приводит к ошибке при запуске"
    },
    "ErrorContains": {
      "description": "сообщение об ошибке, содержащее {{x|UnicodeString}}"
    }
  },
  "SetInput": {
    "Equals": {
      "description": "равно {{x|SetOfUnicodeString}}"
    },
    "IsSubsetOf": {
      "description": "является собственным подмножеством {{x|SetOfUnicodeString}}"
    },
    "IsSupersetOf": {
      "description": "является собственным подмножеством {{x|SetOfUnicodeString}}"
    },
    "HasElementsIn": {
      "description": "имеет общие элементы с {{x|SetOfUnicodeString}}"
    },
    "HasElementsNotIn": {
      "description": "имеет элементы не в {{x|SetOfUnicodeString}}"
    },
    "OmitsElementsIn": {
      "description": "опускает некоторые элементы {{x|SetOfUnicodeString}}"
    },
    "IsDisjointFrom": {
      "description": "не имеет общих элементов с {{x|SetOfUnicodeString}}"
    }
  },
  "TextInput": {
    "Equals": {
      "description": "равно {{x|NormalizedString}}"
    },
    "CaseSensitiveEquals": {
      "description": "равно {{x|NormalizedString}}, принимая во внимание"
    },
    "StartsWith": {
      "description": "начинается с {{x|NormalizedString}}"
    },
    "Contains": {
      "description": "содержит {{x|NormalizedString}}"
    },
    "FuzzyEquals": {
      "description": "равно {{x|NormalizedString}}, с ошибкой не более одного символа"
    },
    "FuzzyMatches": {
      "description": "похож на {{training_data|SetOfNormalizedString}}"
    }
  }
}
